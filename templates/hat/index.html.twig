{% extends 'base.html.twig' %}

{% block title %}Hello HatController!{% endblock %}

{% block body %}
    <div class="row ">
        <h1>Tous les chapeaux</h1>
        {% for hat in hats %}

            <div class="col-2" >
                <div class="card" style="height: 30vh">
                    <div class="card-body">
                        <img src="https://picsum.photos/80/80" class="card-img-top">

                        <p class="card-text"> Chapeau : {{ hat.name }}</p>
                        <p class="card-text">Le prix :{{ hat.price }} €</p>

                        <a href="{{ path('hat_show', {id: hat.id}) }}">Voir le chapeau</a>
                    </div>

                </div>





            </div>


        {% endfor %}
    </div>


{% endblock %}
